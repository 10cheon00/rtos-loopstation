---
title: 비활성 상태 처리
version: 0.1.0
change_history:
  - date: 2026-07-18
    version: 0.1.0
    summary: 비활성 상태 처리 기능 명세를 작성함
---

# 비활성 상태 처리

## 1. 기능 범위

| 항목 | 내용 |
| --- | --- |
| 기능 ID | `FEAT-STATE-SYSTEM-012` |
| 상위 설계 문서 | `ARCH-STATE-SYSTEM.md` |
| 관련 설계 항목 | `ARCH-STATE-SYSTEM-012` |
| 관련 요구사항 | `REQ-STATE-SYSTEM-006` |
| 주요 목적 | 초기화 전, 종료 중, 오류 상태의 일반 동작을 차단한다. |
| 제외 범위 | 시스템 우선 및 진단 이벤트 |

## 2. 연결된 상위 설계 항목

| 설계 항목 ID | 설계 항목 | 연결 내용 |
| --- | --- | --- |
| `ARCH-STATE-SYSTEM-012` | 비활성 상태 처리 | 일반 이벤트는 대상 상태 머신에 전달하지 않고 `REJECTED`로 처리한다. |

## 3. 목적

초기화 전, 종료 중, 오류 상태의 일반 동작을 차단한다.
이를 위해 일반 이벤트는 대상 상태 머신에 전달하지 않고 `REJECTED`로 처리한다.

## 4. 입력

| 입력 | 설명 |
| --- | --- |
| 현재 시스템 상태 | NOT_INIT, INIT, END 또는 ERROR |
| 일반 StateEvent | 기능 동작 요청 |

## 5. 출력

| 출력 | 설명 |
| --- | --- |
| 상태 처리 결과 | REJECTED |
| 거부 정보 | 현재 상태와 이벤트 type |

## 6. 구현 기준

| 항목 | 기준 |
| --- | --- |
| 설계 결정 | 일반 이벤트는 대상 상태 머신에 전달하지 않고 `REJECTED`로 처리한다. |
| 무변경 | 거부된 일반 이벤트는 다른 상태 머신과 canonical state를 변경하지 않아야 한다. |
| 오류 처리 | 입력이나 현재 상태가 유효하지 않으면 canonical state를 변경하지 않고 명시적 오류 또는 거부 결과를 반환해야 한다. |
| 실행 문맥 | 상태 관리 태스크의 이벤트 처리 경로에서 무제한 blocking과 동적 메모리 할당 없이 실행해야 한다. |

## 7. 완료 기준

| 기준 | 확인 방법 |
| --- | --- |
| 비활성 차단 | 각 비RUNNING 상태에서 일반 이벤트가 REJECTED인지 확인한다. |
| 오류 경로 | 잘못된 입력에서 상태가 훼손되지 않고 정의된 실패 결과가 반환되는지 확인한다. |
| 설계 추적 | 구현과 시험이 `ARCH-STATE-SYSTEM-012`의 설계 결정을 만족하는지 검토한다. |

## 8. 구현 및 검증 상태

| 항목 | 상태 | 날짜 | 비고 |
| --- | --- | --- | --- |
| 구현 | 미구현 | - | 기능 명세에 따른 구현이 필요하다. |
| 검증 | 미검증 | - | 구현 완료 후 완료 기준에 따라 검증한다. |

